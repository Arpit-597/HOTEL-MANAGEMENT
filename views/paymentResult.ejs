<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Payment Status</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #e0f7fa, #ffffff);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
    }

    .card {
      background: #ffffff;
      padding: 40px 30px;
      border-radius: 16px;
      max-width: 500px;
      width: 100%;
      box-shadow: 0 8px 28px rgba(0, 0, 0, 0.1);
      text-align: center;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .card h1 {
      color: #2e7d32;
      font-size: 2rem;
      margin-bottom: 15px;
    }

    .booking-info {
      text-align: left;
      margin-top: 25px;
    }

    .booking-info p {
      font-size: 1.05rem;
      margin: 10px 0;
      color: #444;
    }

    .booking-info h2 {
      font-size: 1.3rem;
      margin-bottom: 10px;
      color: #333;
    }

    .btn-group {
      margin-top: 30px;
    }

    .btn {
      display: inline-block;
      margin: 8px;
      padding: 12px 24px;
      background-color: #0288d1;
      color: #fff;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      text-decoration: none;
      transition: background 0.3s ease;
    }

    .btn:hover {
      background-color: #0277bd;
    }

    .icon {
      font-size: 48px;
      color: #2e7d32;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="card">
    <% if (success) { %>
      <div class="icon">✅</div>
      <h1>Payment Successful! Your booking is confirmed.</h1>

      <div class="booking-info">
        <h2>Booking Details:</h2>
        <p><strong>Room:</strong> <%= booking.room.title %></p>
        <p><strong>Check-in:</strong> <%= booking.checkInDate.toDateString() %></p>
        <p><strong>Check-out:</strong> <%= booking.checkOutDate.toDateString() %></p>
        <p><strong>Guests:</strong> <%= booking.guestCount %></p>
        <p><strong>Total Paid:</strong> ₹<%= booking.totalAmount.toFixed(2) %></p>
        <p><strong>Status:</strong> <%= booking.status %></p>
      </div>

      <div class="btn-group">
        <a href="/user/bookings" class="btn" target="_blank">🏠 Back to Rooms</a>
        <a href="/user/bookings" class="btn" target="_blank">📖 View My Bookings</a>
      </div>
    <% } else { %>
      <div class="icon" style="color: #c62828;">❌</div>
      <h1>Payment Failed!</h1>
      <p>Please try again or contact support.</p>
      <div class="btn-group">
        <a href="/user/bookings" class="btn">Back to Rooms</a>
      </div>
    <% } %>
  </div>
</body>
</html>
